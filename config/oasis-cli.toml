[telemetry]
log_level = "info"
log_format = "text"
log_no_ansi = false

server_url = "https://127.0.0.1:50051"

[grpc_tls]
# CA 证书路径（用于验证服务器证书）
ca_cert = "certs/grpc-ca.pem"
# 客户端证书路径
client_cert = "certs/grpc-client.pem"
# 客户端私钥路径
client_key = "certs/grpc-client-key.pem"
